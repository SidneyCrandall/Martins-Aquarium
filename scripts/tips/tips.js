export const tipList = (tipListObj) => {
    return(
              `<div class="tipList-card">
                <ul>
                <li class="tipList-details"> ${tipListObj.Tip}</li>
                </ul>
            </div>`
    )
}